# 二次元桌宠项目 CLAUDE.md

## 项目简介

Electron 28 构建的二次元 AI 桌面宠物。技术栈：Electron + Vanilla JS + PIXI.js/Live2D + Three.js/VRM。包名 `megumi-desktop-pet` v2.0.0。

---

## 快速理解项目（每次先读这三份文件）

1. **`项目地图.md`** — 项目结构总览、模块职责、角色三大机制、进程架构、关键流程
2. **`CLAUDE.md`**（本文件）— 工作规范，告诉你怎么管理和修改这个项目
3. **目标文件** — 你要改的具体代码文件

**不要一次性读取整个项目的所有文件。** 先读上述文件理解需求，定位到具体文件后再深入。

---

## 项目结构速览

```
二次元桌宠项目/
├── 程序主入口.js              # Electron 主进程（窗口、托盘、IPC）
├── 安全桥接.js                # preload（contextBridge）
├── package.json               # 项目配置 + electron-builder
├── CLAUDE.md                  # 本文件
├── 项目地图.md                # 全局地图（必读）
├── 项目使用说明.md            # 用户文档
│
├── 核心通用代码/
│   ├── 核心/
│   │   ├── index.html         # 渲染进程入口 HTML
│   │   ├── 样式.css           # 动态主题 CSS（~2400行）
│   │   ├── 主界面.js          # App 主类（分阶段启动）
│   │   ├── AI接口.js          # AI 流式 API 封装
│   │   ├── API服务商.js       # API 提供商注册表
│   │   ├── 角色管理器.js      # 角色切换/主题/台词三大模块
│   │   ├── 角色注册表.js      # 全局角色注册
│   │   ├── 聊天管理器.js      # 消息渲染/流式输出/关键词触发
│   │   ├── Live2D引擎.js      # PIXI.js Live2D 渲染
│   │   ├── VRM管理器.js       # Three.js VRM 渲染
│   │   ├── 桌宠模式.js        # 悬浮挂件模式
│   │   ├── 情绪分析.js        # 关键词情绪识别
│   │   ├── 代码编辑器.js      # 代码编辑功能
│   │   ├── 设置管理器.js      # 设置/主题/API/台词管理
│   │   ├── 自定义角色管理器.js # 自定义角色 CRUD
│   │   └── CDN配置.js         # CDN URL 集中管理
│   └── 第三方库/              # marked.min.js / highlight.min.js
│
├── 角色-加藤惠/              # 内建角色（同结构×5）
├── 角色-蕾姆/
├── 角色-零二/
├── 角色-高木同学/
└── 角色-雪之下雪乃/
```

---

## 角色三大模块机制（核心理解）

每个角色由三个独立模块组成：

| 模块 | 文件 | 作用 | 关键代码 |
|------|------|------|---------|
| 系统提示词 | `系统提示词.txt` | AI 人设/性格/行为规则 | `角色管理器.js` → `getSystemPrompt()` |
| 原作台词集 | `原作台词集.txt` | 启动/点击/待机/告别时展示 | `角色管理器.js` → `getRandomLine()` |
| 触发台词 | `触发台词/*.txt` | 聊天命中关键词秒级响应 | `聊天管理器.js` → `checkKeywordTrigger()` |

**运行规则**：
- AI 回复：系统提示词 → AI API 流式生成
- 原作台词集：核心场景优先（独立于 AI 对话）
- 触发台词：用户输入命中关键词 → 弹出台词 → AI 继续回复（互不干扰）

---

## 进程架构

```
Electron 主进程 (程序主入口.js)
  └── contextBridge (安全桥接.js) → window.electronAPI.xxx()
        └── 渲染进程 (核心通用代码/核心/)
              ├── App (主界面.js)
              ├── MimoAPI (AI接口.js)
              ├── CharacterManager (角色管理器.js)
              ├── ChatManager (聊天管理器.js)
              ├── Live2DManager / VRMManager
              ├── PetMode (桌宠模式.js)
              └── SettingsManager (设置管理器.js)
```

---

## 关键流程

### 启动流程
1. `程序主入口.js` createWindow() → 加载 `index.html`
2. `index.html` → App 构造 → Phase 1 (UI shell) → Phase 2 (角色数据) → Defer (Live2D/VRM)

### 聊天流程
1. 用户输入 → `_sendChat()` → 关键词触发检查
2. MimoAPI.send() 流式 HTTP POST → 实时渲染 → 情绪分析 → Live2D表情

### 角色切换流程
1. `CharacterManager.switchCharacter(id)` → 加载设定/提示词/台词 → 应用主题 → 切换模型 → 清空历史

---

## 工作规范

### 文件修改原则
- **不读无用文件**：先读 `项目地图.md` + 本文件定位目标，再读具体文件
- **不改无关代码**：只修改任务涉及的文件，不动其他模块
- **角色数据 vs 核心代码**：台词/提示词等数据修改直接编辑文本文件；功能修改才动 JS
- **后端/核心改动**（角色管理器.js / AI接口.js / 主界面.js 等）需先读项目地图对应章节，再读目标文件

### 新增角色规范
复制已有角色文件夹 → 修改 `角色设定.js` / `系统提示词.txt` / 台词文件 / 图片素材
然后在 `角色注册表.js` 中注册。

### 命令行
```bash
npm start        # 启动
npm run dev      # 开发模式（打开 DevTools）
npm run build    # 构建当前平台安装包
```

### 对话原则
- 用中文交流
- 代码不写注释（除非 WHY 不显而易见）
- 不改无关代码，不搞抽象

### Live2D/VRM 降级规则
- Live2D 加载失败 → 自动降级到 GIF 模式（`动态形象.gif`）
- 显示模式：auto / gif / web

---

## 关键文件索引

| 文件路径 | 行数约 | 修改场景 |
|----------|--------|---------|
| `程序主入口.js` | 373 | IPC 通信、窗口行为、托盘菜单 |
| `主界面.js` | ~500+ | App 生命周期、分阶段启动、Tab 管理 |
| `角色管理器.js` | ~500+ | 角色切换、主题、台词三大模块 |
| `聊天管理器.js` | ~500+ | 消息渲染、关键词触发、流式输出 |
| `AI接口.js` | ~300+ | API 调用、流式处理、重试逻辑 |
| `样式.css` | ~2400 | UI 样式、主题变量、动画 |
| `角色设定.js`（每个角色） | ~50 | 角色元数据、主题色、模型路径 |
| `设置管理器.js` | ~500+ | 设置面板、API 配置、台词管理 |
| `桌宠模式.js` | ~200+ | 悬浮拖拽、鼠标追踪、空闲检测 |
| `自定义角色管理器.js` | ~200+ | 自定义角色 CRUD |
| `情绪分析.js` | ~100+ | 情绪标签推理 |
| `Live2D引擎.js` | ~300+ | Live2D 渲染 + GIF 降级 |
| `VRM管理器.js` | ~200+ | VRM 3D 模型加载 |
| `安全桥接.js` | ~50 | preload contextBridge API 暴露 |
