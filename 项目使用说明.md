# 二次元桌宠

一个可爱的二次元AI桌面桌宠应用，支持多角色切换、Live2D动态形象、智能聊天和代码辅助功能。

## 功能特性

- **多角色切换** - 加藤惠、蕾姆、02三大经典角色，一键切换
- **Live2D动态形象** - 可爱的二次元角色，支持表情和动作
- **智能聊天** - 对接AI大模型，每个角色都有专属性格和台词
- **代码辅助** - AI帮你写代码，支持多种编程语言
- **透明悬浮** - 无边框透明窗口，始终置顶
- **丰富设置** - 自定义API、透明度、模型等
- **精美界面** - 每个角色独立主题色，渐变色彩设计

## 角色列表

| 角色 | 作品 | 特色 |
|------|------|------|
| 加藤惠 | 路人女主的养成方法 | 平淡吐槽、温柔包容、路人女主 |
| 蕾姆 | Re:从零开始的异世界生活 | 温柔贤惠、坚定守护、鬼族女仆 |
| 02 | DARLING in the FRANXX | 大胆奔放、天真妖媚、龙族少女 |

## 快速开始

### 环境要求

- Node.js 16+
- npm 或 yarn
- AI API Key

### 安装步骤

```bash
npm install
npm start
```

### 配置API

1. 点击标题栏的 `⚙` 按钮打开设置
2. 输入API地址和Key
3. 选择模型
4. 点击保存

## 使用说明

### 基本操作

- **拖拽移动** - 拖动标题栏移动窗口
- **切换角色** - 点击标题栏的 `👤` 按钮
- **最小化** - 点击 `─` 按钮
- **关闭** - 点击 `✕` 按钮（隐藏到托盘）
- **快捷键** - `Ctrl+Shift+P` 显示/隐藏窗口

### 角色互动

- **单击角色** - 角色会随机回应
- **连续点击** - 角色会吐槽或表达小情绪
- **长时间无操作** - 角色会主动找你聊天
- **生日彩蛋** - 角色生日当天会有特殊对话

## 项目结构

```
二次元桌宠项目/
├── 程序主入口.js               # Electron主进程
├── 安全桥接.js                 # 预加载脚本
├── package.json               # 项目配置
│
├── 核心通用代码/               # 共享代码和资源
│   ├── 核心/
│   │   ├── index.html         # 主界面
│   │   ├── 主界面.js          # 渲染进程主逻辑
│   │   ├── 样式.css           # 全局样式
│   │   ├── 角色管理器.js      # 角色切换、主题应用
│   │   ├── 角色注册表.js      # 角色注册表初始化
│   │   ├── AI接口.js          # AI API调用
│   │   ├── Live2D引擎.js      # Live2D管理
│   │   └── API服务商.js       # API服务商配置
│   ├── 第三方库/               # marked.js、highlight.js等
│   └── 默认素材/               # 默认头像和封面
│
├── 角色-加藤惠/                # 角色1：路人女主的养成方法
│   ├── 角色设定.js             # 元数据、主题配色、Live2D配置
│   ├── 系统提示词.txt          # AI人设提示词（可直接编辑）
│   ├── 图片素材/               # 头像和封面图片
│   └── 台词/                   # 各场景台词（每行一句，可直接编辑）
│       ├── 开机.txt、待机.txt、点击.txt、吐槽.txt ...
│
├── 角色-蕾姆/                  # 角色2：Re:从零开始的异世界生活
│   └── （同上结构）
│
└── 角色-零二/                  # 角色3：DARLING in the FRANXX
    └── （同上结构）
```

### 修改角色设定

- **修改台词**：直接编辑对应角色文件夹下 `台词/*.txt` 文件，每行一句
- **修改AI人设**：编辑 `系统提示词.txt`
- **修改主题色**：编辑 `角色设定.js` 中的 `theme` 对象
- **更换头像/封面**：替换 `图片素材/` 下的图片文件

## 许可证

MIT License
