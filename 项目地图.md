# 二次元桌宠项目地图

## 项目概况
- **技术栈**: Electron 28 + Vanilla JS + PIXI.js/Live2D + Three.js/VRM
- **架构**: 主进程 (Electron) + 渲染进程 (浏览器) + 安全桥接 (preload)
- **包名**: megumi-desktop-pet v1.2.0
- **构建**: electron-builder (win/mac/linux)

---

## 一、项目结构总览

```
二次元桌宠项目/
├── 程序主入口.js          # Electron 主进程
├── 安全桥接.js            # preload (contextBridge)
├── package.json            # 项目配置
├── 项目使用说明.md          # 用户文档
├── 项目代码总结与改进方案.md # 技术文档
│
├── 核心通用代码/
│   ├── 核心/
│   │   ├── index.html          # 渲染进程入口 HTML
│   │   ├── 样式.css            # 动态主题 CSS (~800行)
│   │   ├── 主界面.js           # App 主类 (启动分阶段)
│   │   ├── AI接口.js           # AI聊天 API 封装 (流式)
│   │   ├── API服务商.js        # API提供商注册表
│   │   ├── 角色管理器.js       # 角色 / 主题 / 台词管理
│   │   ├── 角色注册表.js       # 全局角色注册对象
│   │   ├── 聊天管理器.js       # 消息渲染 / 流式输出
│   │   ├── Live2D引擎.js       # PIXI.js Live2D 渲染
│   │   ├── VRM管理器.js        # Three.js VRM 渲染
│   │   ├── 桌宠模式.js         # 悬浮挂件模式
│   │   ├── 情绪分析.js         # 关键词情绪识别
│   │   ├── 代码编辑器.js       # 代码编辑功能
│   │   ├── 设置管理器.js       # 设置 / 主题 / API配置
│   │   ├── 自定义角色管理器.js # 自定义角色 CRUD
│   │   └── CDN配置.js          # CDN URL 集中管理
│   └── 第三方库/
│       ├── marked.min.js       # Markdown 渲染
│       ├── highlight.min.js    # 代码高亮
│       └── 代码主题.css        # 代码高亮主题
│
├── 角色-加藤惠/         # 内建角色 1
├── 角色-蕾姆/           # 内建角色 2
├── 角色-零二/           # 内建角色 3
└── .claude/settings.json    # Claude Code 配置
```

---

## 二、进程架构与通信

```
┌──────────────────────────────────────────────────┐
│  Electron 主进程 (程序主入口.js)                    │
│  ├── 窗口管理 (BrowserWindow)                     │
│  ├── 托盘菜单 (Tray)                              │
│  ├── 全局快捷键 (Ctrl+Shift+P)                    │
│  └── IPC Handler:                                 │
│      ├── get-screen-size                          │
│      ├── set-always-on-top / minimize / close      │
│      ├── update-tray-label                        │
│      └── 自定义角色 CRUD (save/export/import)      │
└──────────────┬───────────────────────────────────┘
               │ contextBridge (安全桥接.js)
               │ window.electronAPI.xxx()
┌──────────────▼───────────────────────────────────┐
│  渲染进程 (核心通用代码/核心/)                      │
│  ┌─────────────────────────────────────────────┐  │
│  │  App (主界面.js)                             │  │
│  │  ├── ChatManager  (聊天管理器.js)            │  │
│  │  ├── CodeEditor   (代码编辑器.js)            │  │
│  │  ├── SettingsManager (设置管理器.js)         │  │
│  │  └── PetMode      (桌宠模式.js)              │  │
│  └─────────────────────────────────────────────┘  │
│  MimoAPI (AI接口.js)    ← 流式 HTTP 请求          │
│  CharacterManager (角色管理器.js)                  │
│  Live2DManager (Live2D引擎.js)                    │
│  VRMManager (VRM管理器.js)                         │
│  CustomCharacterManager (自定义角色管理器.js)       │
└──────────────────────────────────────────────────┘
```

---

## 三、核心模块详解

### 1. 程序主入口.js
- **职责**: Electron 主进程入口
- **关键功能**:
  - 单实例锁 (requestSingleInstanceLock)
  - 创建透明无边框窗口 (400×600, 右下角)
  - 系统托盘 (Tray) 带右键菜单
  - 全局快捷键 Ctrl+Shift+P 显示/隐藏
  - IPC: 自定义角色的 创建/导出/导入/删除
  - IPC: 窗口控制 (置顶/最小化/关闭)

### 2. 主界面.js (App 类)
- **职责**: 渲染进程主控制器
- **启动策略**: 分阶段初始化
  - Phase 1: 立即渲染 UI shell, 绑定事件, 显示加载动画
  - Phase 2 (setTimeout 10ms): 加载角色数据, 初始化桌宠模式
  - Defer (setTimeout 600ms): 加载 Live2D/VRM 等重型资源
- **管理**: ChatManager, CodeEditor, SettingsManager, PetMode

### 3. AI接口.js (MimoAPI 类)
- **职责**: 与 AI API 通信
- **特性**:
  - 流式输出 (streaming) + 智能分割 (思考过程折叠)
  - 自动重试 (2次), 多提供商支持
  - 对话历史管理 (最多30条)
  - 响应模式: instant / thoughtful
  - 提示词模式: auto / fixed / custom

### 4. API服务商.js
- **注册表**: 支持 豆包(Doubao) / DeepSeek / Mimo / Ollama(本地)
- 每个服务商定义: baseUrl, models[], needsKey, description

### 5. 角色管理器.js (CharacterManager)
- **职责**: 角色切换、主题应用、台词管理
- **关键方法**:
  - switchCharacter(id) → 加载角色配置、主题、Live2D
  - loadCharacters() → 加载所有内建角色
  - loadSavedCharacter() → 恢复上次使用的角色
  - getSystemPrompt() / getRandomLine() → 获取提示词/台词
- **主题系统**: THEME_CSS_MAP 将 JS 属性 → CSS 变量, 动态注入 :root
- **台词缓存**: 首次 fetch 后本地缓存, 避免重复请求

### 6. 聊天管理器.js (ChatManager)
- **职责**: 消息渲染、流式打字效果
- **特性**:
  - Markdown 渲染 (marked.js) + 代码高亮
  - 思考过程折叠 (<think> 标签)
  - 颜文字随机显示
  - 消息滚动到底部追踪
  - 最大 50 条消息自动修剪

### 7. 渲染引擎 (二选一)
| 引擎 | 文件 | 技术 | 适用场景 |
|------|------|------|---------|
| Live2D 引擎 | Live2D引擎.js | PIXI.js + pixi-live2d-display | Live2D 模型 |
| VRM 管理器 | VRM管理器.js | Three.js + @pixiv/three-vrm | VRM 3D 模型 |
| GIF 降级 | Live2D引擎.js | 纯 GIF 显示 | 无模型/低性能 |

- **自动降级**: Live2D失败 → GIF模式 (动态形象.gif)
- **显示模式**: auto / gif / web (网页模式)

### 8. 桌宠模式.js (PetMode)
- **职责**: 悬浮挂件模式 (小窗口/拖拽)
- **特性**:
  - 拖拽移动 (mouse drag)
  - 鼠标追踪 (角色视线跟随)
  - 空闲检测 (2分钟无操作 → 随机台词)
  - 双击切换大/小窗口
  - 网页嵌入模式 (web mode)

### 9. 情绪分析.js
- **职责**: AI 回复文本 → 情绪标签
- **算法**: 加权关键词评分
- **情绪类型**: tsukkomi(吐槽) / gentle(温柔) / jealous(吃醋) / thinking(思考) 等
- **影响**: Live2D 表情、主题色微调

### 10. 代码编辑器.js (CodeEditor)
- **职责**: 写代码标签页
- **特性**: 行号、Tab缩进、语法模板(HTML/JS/CSS/Python/Java等)、复制/运行按钮

### 11. 设置管理器.js (SettingsManager)
- **职责**: 设置面板管理
- **功能**: API Key配置、模型选择、角色提示词编辑、角色导入/导出/删除、自定义角色创建

### 12. 自定义角色管理器.js (CustomCharacterManager)
- **职责**: 管理用户创建的自定义角色
- **流程**: loadCustomCharacters() → 从磁盘读取 registry.json → 生成主题 → 注入 CHARACTER_REGISTRY
- **theme 生成**: 基于 primaryColor 自动生成全套配色

---

## 四、角色数据结构

### 每个角色的目录结构
```
角色-XXX/
├── 角色设定.js       # 角色元数据 + 主题配色（注册到 CHARACTER_REGISTRY）
├── 系统提示词.txt     # AI 系统提示词
├── 台词/             # 情景台词 txt 文件
│   ├── 开机.txt, 待机.txt, 点击.txt, 告别.txt
│   ├── 吐槽.txt, 温柔.txt, 吃醋.txt, 特殊.txt
│   └── 天气.txt, 夸奖.txt, 晚安.txt, 鼓励.txt (视角色而定)
└── 图片素材/
    ├── 头像.png       # 聊天头像
    ├── 封面.png       # 加载封面 / 降级显示
    └── 动态形象.gif   # GIF降级模式（可选）
```

### 内建角色
| ID | 名称 | 作品 | 特色台词 |
|----|------|------|---------|
| megumi | 加藤惠 | 路人女主的养成方法 | 吐槽/温柔/吃醋 |
| rem | 蕾姆 | Re:从零开始的异世界生活 | 温柔/料理/自我怀疑 |
| zerotwo | 零二 | DARLING in the FRANXX | 调情/孤独/特殊 |

### 角色注册对象 (CHARACTER_REGISTRY)
```js
{
  id, name, nameJa, series, seriesJa, cv,
  birthday: { month, day },
  tagline, description,
  avatar, cover,           // 图片路径
  theme: { ... },          // 50+ CSS 变量
  live2d: {                // Live2D 配置
    fallbackImage, modelPath
  },
  systemPrompt: null,      // 运行时加载
  lines: {}                // 运行时缓存
}
```

---

## 五、关键流程

### 启动流程
1. `程序主入口.js` createWindow() → 加载 `index.html`
2. `index.html` 加载 → `主界面.js` App 构造
3. Phase 1: UI shell + loading 动画 (同步)
4. Phase 2: CharacterManager 加载角色 (异步)
5. Defer: Live2D/VRM 引擎初始化 (600ms 后)
6. PetMode 初始化 → 桌宠悬浮模式

### 聊天流程
1. 用户输入 → ChatManager.sendMessage()
2. MimoAPI.send() → 流式 HTTP POST
3. 实时渲染流式文本 → 结束后分割 <think>
4. 情绪分析 → Live2D表情变化
5. 对话历史追加

### 角色切换流程
1. 用户选择角色 → CharacterManager.switchCharacter(id)
2. 加载角色设定.js + 系统提示词.txt + 台词文件
3. 应用主题配色 (CSS 变量注入)
4. 切换 Live2D/VRM/GIF 模型
5. 更新托盘图标和标题
6. 清空对话历史

---

## 六、关键依赖

| 依赖 | 用途 | 加载方式 |
|------|------|---------|
| Electron 28 | 桌面应用框架 | npm (devDependency) |
| PIXI.js 7.3 | Live2D 渲染 | CDN (jsdelivr) |
| pixi-live2d-display 0.4 | Live2D 显示 | CDN |
| Three.js 0.137 | VRM 3D 渲染 | CDN |
| three-vrm 2.0 | VRM 模型加载 | CDN |
| marked.js | Markdown → HTML | 本地第三方库 |
| highlight.js | 代码语法高亮 | 本地第三方库 |

---

## 七、构建与运行

```bash
npm start        # 启动 (electron .)
npm run dev      # 开发模式 (打开 DevTools)
npm run build    # 构建当前平台安装包
npm run build:win  # Windows 安装包
npm run build:mac  # macOS 安装包
```
